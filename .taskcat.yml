project:
  name: connect-integration-egain-solve
  owner: quickstart@amazon.com
  package_lambda: false
  regions:
    - us-east-1
tests:
  defaults:
    parameters: {}
    regions:
      - us-east-1
    template: templates/egain-solve.template.yaml
